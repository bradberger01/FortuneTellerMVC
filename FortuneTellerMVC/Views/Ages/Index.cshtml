@model IEnumerable<FortuneTellerMVC.Models.Age>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Age1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Age1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AgeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.AgeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AgeID })
        </td>
    </tr>
}

</table>
